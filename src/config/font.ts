import { Onest, Montserrat_Alternates } from 'next/font/google';

export const onest = Onest({ subsets: ['latin'] });

export const titleFont = Montserrat_Alternates({
  subsets: ['latin'],
  weight: ['500', '700'],
});
